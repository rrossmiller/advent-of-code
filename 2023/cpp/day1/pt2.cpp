#include <cstdio>
#include <fstream>

using std::ifstream;
using std::printf;

void pt2(ifstream &dataFile) {
    int ans;
    printf("%s\n", "hello pt 2");

    printf("ans is correct: %s\n", ans == 5593 ? "true" : "false");
}
